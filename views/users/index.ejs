<h1>All USERS</h1>


<% if (users.length === 0) { %>
    <p>No users available.</p>
<% } else { %>
    <ul>
        <% users.forEach(user => { %>
            <li>
                <a href="../users/show/<%= user._id %>"><%= user.username %></a>
            </li>
        <% }) %>
    </ul>
<% } %>
